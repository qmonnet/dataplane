[package]
name = "dataplane-tracectl"
version = "0.1.0"
edition = "2024"
publish = false
license = "Apache-2.0"

[dependencies]
tracing = { workspace = true }
tracing-subscriber = { workspace = true }
