[package]
name = "dataplane-interface-manager"
version = "0.0.1"
edition = "2024"
license = "Apache-2.0"
publish = false

[features]
bolero = []

[dependencies]
# internal

# external
rtnetlink = { workspace = true, features = ["tokio"] }

[dev-dependencies]
# internal

# external
