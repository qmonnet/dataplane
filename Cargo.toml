[workspace]

members = [
	"dataplane",
	"dpdk",
	"dpdk-sys",
	"dpdk-sysroot-helper",
	"errno",
	"net",
]
resolver = "2"

[workspace.dependencies]

# Internal

dpdk = { path = "./dpdk" }
dpdk-sys = { path = "./dpdk-sys" }
dpdk-sysroot-helper = { path = "./dpdk-sysroot-helper" }
errno = { path = "./errno" }
net = { path = "./net" }

# External
bindgen = { version = "0.71.1", default-features = false, features = [] }
# Bolero should always be built with arbitrary support since that is our principal means of using it.
# There is also no need to build without alloc support in any case where we need property tests.
bolero = { version = "0.12.0", default-features = false, features = ["arbitrary", "alloc"] }
doxygen-rs = { version = "0.4.0", default-features = false, features = [] }
etherparse = { version = "0.17.0", default-features = false, features = [] }
rand = { version = "0.8.5", default-features = true }
serde = { version = "1.0.213", default-features = false }
thiserror = { version = "2.0.11", default-features = false, features = [] }
# attribute feature is so commonly used that we should just leave it on globally
tracing = { version = "0.1.40", default-features = false, features = ["attributes"] }

